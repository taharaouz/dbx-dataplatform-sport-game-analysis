# This is a Databricks asset bundle definition for dbx_dataplatform_sport_game_analysis.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dbx_dataplatform_sport_game_analysis
  uuid: 4bbddb57-1d08-4b0e-a10c-abf9426f098b

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  env_prefix:
    description: Environment catalog prefix.

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-6e272d3f-3f61.cloud.databricks.com # A modifier en mettant le lien vers votre workspace personnel
    variables:
      env_prefix: dev_
      
